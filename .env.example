# Copiar a .env y rellenar los valores reales

# Django
SECRET_KEY=cambia-esto-por-una-clave-larga-aleatoria-en-produccion
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Base de datos (SQLite en local, cambiar a PostgreSQL en producción)
# DATABASE_URL=postgresql://usuario:password@localhost/europeptiva_db

# Email (Gmail con App Password, o Mailjet en producción)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tuemail@gmail.com
# EMAIL_HOST_PASSWORD=xxxx-xxxx-xxxx-xxxx
# DEFAULT_FROM_EMAIL=EuroPeptiva <noreply@europeptiva.com>
# ADMIN_EMAIL=tuemail@gmail.com

# Datos bancarios para transferencias (aparecen en emails de confirmación)
# BANK_IBAN=ES00 0000 0000 0000 0000 0000
# BANK_HOLDER=Nombre Empresa S.L.
